{
  "name": "youtube-speedx",
  "version": "3.0.0",
  "private": true,
  "license": "MIT",
  "description": "A userscript to enhance video viewing with custom speed, resolution control, H.264 forcing, and a settings UI.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run build && playwright test",
    "test:e2e:real": "RUN_REAL_E2E=1 npm run build && RUN_REAL_E2E=1 playwright test tests/e2e/real"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.3",
    "esbuild": "^0.27.3",
    "jsdom": "^28.1.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
